# Logitech USB Camera Defect Detection App

A desktop AI-based defect detection app using a Logitech webcam and OpenCV + PyQt5.

## Features

- Live camera preview
- Manual capture and AI classification (OK/NG)
- Image saving to `captures/`
- Excel logging to `log.xlsx`

## Installation

```bash
pip install -r requirements.txt
python main.py
